# dependencies (there are none, but just in case)
node_modules/
package-lock.json

# editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# the app needs no secrets, but never let one in by accident
.env
.env.local

# scratch
*.log
tmp/

# Playwright's leavings from `npm run test:mobile`
test-results/
playwright-report/

# diagnostic frames saved by the scanner
testdata/
